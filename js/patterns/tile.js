define([
  'jquery',
  'js/patterns/base'
], function($, Base, Parser) {
  "use strict";

  var Tile = Base.extend({
    name: "tile",
    defaults: {
    },
    init: function() {
      var self = this;
    }
  });

  return AutoTOC;

});
